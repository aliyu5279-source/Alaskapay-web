# Ensure package.json is always committed as text
package.json text eol=lf

# Shell scripts should use LF line endings
*.sh text eol=lf

# Ensure these files are never ignored
package.json !filter !diff
package-lock.json !filter !diff
